Make sure you have the SDL2 and SDL2_mixer source folder in jni.
They must be called SDL and SDL_mixer respectively.

Then launch "./gradlew build" or compile.sh to start compilation.
Make sure you have API 16 installed